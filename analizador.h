#ifndef ANALIZADOR_H
#define ANALIZADOR_H
#include <string>
#include <vector>
using namespace std;

class Analizador
{
private:
    vector<string> pila ;
public:
    Analizador(vector<string>);
};

#endif // ANALIZADOR_H
